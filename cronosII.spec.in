%define name		@PACKAGE@
%define version		@VERSION@
%define sysconfdir	/etc
%define RELEASE		0
%define rel	%{?CUSTOM_RELEASE} %{!?$CUSTOM_RELEASE:%RELEASE}

Summary: Cronos II is a powerful mail client for GNOME.
Name: %{name}
Version: %{version}
Release: %rel
Copyright: GPL
Group: Applications/Internet
URL: http://cronosII.sourceforge.net
Source: %{name}-%{version}.tar.gz
Requires: gnome-libs >= 1.0.0
Packager: Yves Mettier <ymettier@libertysurf.fr>
BuildRoot: %{_tmppath}/%{name}-root
Prefix: %{_prefix}

%description
Cronos II is a powerful GNOME mail client that uses its own database storage
format to ensure a high level of performance.
If you would like to help with the development,
contact Cronos II (Pablo Fernández Navarro) <cronosII@users.sourceforge.net>

%description -l fr
Cronos II est un puissant lecteur de mails pour GNOME. Il utilise son propre
format de stockage des mails afin d'avoir les hautes performances qu'il est
capable d'atteidre.
Si vous voulez aider au developpement, contactez Cronos II (Pablo Fernández
Navarro) <cronosII@users.sourceforge.net>

%package devel
Summary: Developer files for CronosII
Group: Applications/Internet
Requires: gnome-libs-devel >= 1.0.0

%description devel
Developer files for CronosII

%description devel -l fr
Fichiers de developpement pour CronosII.

%prep
%setup -q

%build
%configure --disable-debug --disable-gtkxmhtml
make

%install
rm -rf $RPM_BUILD_ROOT
%makeinstall

%clean
rm -rf $RPM_BUILD_ROOT

%files
%defattr(-,root,root)
%doc ABOUT-NLS AUTHORS COPYING ChangeLog FEATURES INSTALL NEWS README doc/C/cronosII/*.sgml TODO
%{_bindir}/cronos
%{_datadir}/gnome/apps/Internet/cronosII.desktop
%{_datadir}/gnome/apps/Applications/c2addrbook.desktop
%{_datadir}/gnome/help/cronosII/*
%{_datadir}/pixmaps/*
%{_datadir}/locale/*/*/*
%{_datadir}/mc/templates/*
%{_datadir}/cronosII-scratch/
%{_mandir}/*

%files devel
%defattr(-,root,root)
%{_bindir}/cronosII-config
%{_includedir}/cronosII/*

